interface:
  display_name: "Agent Runtime"
  short_description: "Control multinode runtime clusters"
  default_prompt: "Use $agent-runtime to inspect the cluster and run a safe smoke test across connected nodes."
